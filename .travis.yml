language: php
php:
  - '8.1'

services:
  - postgresql

script:
  - composer install
  - yarn install
  - yarn encore dev
  - php bin/console doctrine:database:create --env=test
  - vendor/bin/phpunit
